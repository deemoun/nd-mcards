<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.app.deemounus.musiccards.AboutApp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textColor="@color/primary_text"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:contentDescription="@string/about_app"
        android:text="@string/about_app"
        android:id="@+id/textView2"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true" />

    <Button
        android:text="@string/show_license"
        android:layout_width="200dp"
        android:theme="@style/AppButtons"
        android:textColor="@color/primary_text"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView2"
        android:layout_alignParentStart="true"
        android:layout_marginTop="13dp"
        android:onClick="showLicensePressed"
        android:id="@+id/showLicense" />
    <Button
        android:text="@string/library_usage"
        android:layout_width="200dp"
        android:theme="@style/AppButtons"
        android:textColor="@color/primary_text"
        android:layout_height="wrap_content"
        android:layout_below="@+id/showLicense"
        android:layout_alignParentStart="true"
        android:layout_marginTop="13dp"
        android:onClick="showLibrariesPressed"
        android:id="@+id/showLicenseLibraries" />
</RelativeLayout>
